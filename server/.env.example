# SQLite database path (default: server/dedup.db when running from repo root)
# DATABASE_URL=sqlite:///./dedup.db

# Optional: webhook URLs for activation
# CLAY_WEBHOOK_URL=https://your-clay-webhook.example.com/...
# N8N_WEBHOOK_URL=https://your-n8n-webhook.example.com/...
